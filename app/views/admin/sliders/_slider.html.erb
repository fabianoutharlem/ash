<tr>
  <td><%= slider.name %></td>
  <td><%= slider.location %></td>
  <td><%= slider.created_at.strftime('%Y-%m-%d - %H:%M') %></td>
  <td>
    <%= link_to 'Aanpassen', edit_admin_slider_path(slider), class: 'btn btn-primary' %>
    <%= link_to 'Slides beheren', admin_slider_slides_path(slider), class: 'btn btn-default' %>
  </td>
</tr>